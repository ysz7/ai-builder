{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The webview may call core_request, and open a native folder picker. Everything else is a method in the Python core -- a picker is the one thing that cannot be, because it is an operating-system window rather than a capability. The sidecar is spawned from Rust, so the shell plugin needs no webview-facing permission.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
