{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The webview may call core_request and nothing else. The sidecar is spawned from Rust, so the shell plugin needs no webview-facing permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
