"""Test configuration.

Its presence is what puts the project root on `sys.path`, so `import rag` works when
pytest is run from anywhere -- the ordinary arrangement, and nothing to do with the
builder.
"""
