[project]
name = "bp"
version = "0.1.0"
description = "Builder primitives: inert markup for AST-addressable Python nodes."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bp"]
